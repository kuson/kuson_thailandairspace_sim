# Thai Airspace Sim — Specification

> **Status:** Phase 1 + Betterment-1 (2026-05-28) + Betterment-2 (2026-05-29, in progress). Thailand-wide catalog, Bangkok-centered projection, dual flight modes, second-order physics, SRTM-backed AGL terrain, atmospheric sky, advisory altitude system with optional Strict-CAAT enforcement.
> **License:** MIT.
> **Disclaimer:** Educational visualization only. Not for flight planning. Data may be approximate or out of date. Consult CAAT and current AIP Thailand.

---

## 1. Purpose

A single-page, zero-build, browser-only 3D simulator that teaches a Thailand-based hobbyist or teenager what the colored shapes on Thai aeronautical charts actually mean. Users fly a 6-DoF drone through transparent extruded airspace volumes laid over an OSM ground map.

Not a flight planner. Not a real-time avionics product. The data resolution and the flat-Earth model are explicitly chosen for clarity, not legal compliance.

---

## 2. Scope (Phase 1)

| Aspect | Decision |
|---|---|
| Geographic scope | **Catalog:** 144 airspaces nationwide (airports, TMAs, full ENR 5.1 P/R/D set). **Projection:** equirectangular tangent plane anchored at Bangkok (13.7563°N, 100.5018°E), scaled by `cos(13.7563°)` — accurate to <1% within ~300 km of origin; usable farther with increasing drift |
| Elevation | **SRTM/Mapzen Terrarium-baked terrain grid** (~30 arc-sec, `data/terrain.bin`); **AGL and AMSL are distinct** — HUD shows both. Flat-Y=0 fallback only until the binary loads. (Legacy "flat ground" assumption from pre-Betterment Phase 1 is retired.) |
| Browser support | Desktop only (Chromium, Firefox, Safari with ESM + WebGL2) |
| Build step | None — `index.html` + ES modules + Three.js from CDN via importmap |
| Hosting | Local `python3 -m http.server` or any static HTTP server (file:// blocked by browser security) |
| Startup position | Browser GPS when permitted and inside Thailand bounds; else Bangkok at 200 m AMSL |

Out-of-scope for Phase 1: terrain, weather, NOTAMs, real-time tracking, mobile/touch, multi-user, persistence, sub-metre boundary fidelity for arc/coast segments.

---

## 3. Functional requirements

### 3.1 Scene
- 3D scene rendered with **Three.js r0.170.0** via CDN importmap.
- Sky: **`three.js` `Sky` shader** (Rayleigh/Mie scattering) — see `src/sky.js`. Drives a sun disc sprite + a `DirectionalLight`; fog far-plane is altitude-lerped. **Betterment-2 (§10.4) raises the "fade-to-space" floor** so the blue dome stays vivid up to ~60 km AMSL; only above ~100 km does it darken. (Pre-betterment flat `0x89b4dc` background is retired.)
- Fog: altitude-driven `THREE.Fog`; near/far interpolated by `lerp(altM, 0..15_000)` between sea-level haze and high-altitude clarity.
- Ground (`DynamicGround`): zoom-9 base **7×7 following drone** + zoom-11 detail 5×5; detail tiles at **Y=0.4**, base at Y=0 (prevents z-fight streaks). Fallback plane follows drone.
- Compass: N/S/E/W at **200 km** on the horizon ring **around the aircraft** (sprites + poles reposition each frame at drone position + world cardinal offset); 192 px font on 12 km sprites.
- Camera: fixed `near=2`, `far=600_000` (do not mutate clip planes per frame while panning).

### 3.2 Airspace volumes
- Defined in `data/airspaces.json` — **144 catalogued volumes** (12+9 AIP-sourced authoritative + 123 approximate; UI badges `AIP` / `approx`). 2026-05-21 audit removed three phantom CTRs (`VTBS-CTR`, `VTCI-CTR`, `VTPR-CTR`) — duplicates / bogus IDs that contributed to visual crowding without adding information.
- Catalog is regenerated by `scripts/build_airspaces.py` (parses `scripts/data/enr51_aip.txt`) merged with hand-curated overrides in `scripts/data/curated_prd.json`. The parser was bug-fixed on 2026-05-20 to correctly handle (a) AIP HTML token-IDs glued against radius/coordinate values and (b) decimal seconds in DDMMSS vertex coordinates. See `journal.md` 2026-05-20 (a) for the full forensic report.
- Built/maintained via `scripts/build_airspaces.py` from AIP ENR 5.1 extract + curated overrides + ENR 2.1 airport CTR/TMA table.
- Categories supported: `CTR`, `TMA`, `Class D`, `Prohibited`, `Restricted`, `Danger`.
- Each volume is defined between `lowerFt` and `upperFt` (converted by `FT_TO_M = 0.3048`).
- **Rendering:** **shaded translucent extruded walls** (Betterment-1 P2.T7) with floor→ceiling gradient; outline pool for edges; depth-stable. **Colorblind hatch pattern** on Prohibited / Restricted, injected world-space into the wall material via `onBeforeCompile` (Betterment-1 P6.T2). The legacy wireframe-cage rendering is retired. **Highlight overlay** (brighter top cap + dashed accent on radar) appears only during identify mode or catalog fly-to overview.
- Optional **3D sprite labels** at each volume centroid (toggle in panel **Display options**). Sub-toggle **Label floor & ceiling (ft)** appends the altitude band to the label text. Labels scale with camera distance, clamped to **14–28 px** on screen.
- Color/opacity policy (top-cap / outline):
  | Category | Color | Opacity |
  |---|---|---|
  | CTR | `0xff3344` red | 0.22 |
  | TMA | `0xff9933` orange | 0.22 |
  | Class D | `0xffe14a` yellow | 0.22 |
  | Prohibited | `0xff0000` solid red | 0.38 |
  | Restricted | `0xa050ff` purple | 0.28 |
  | Danger | `0xff6a1f` deep orange | 0.22 |
- Groups sorted back-to-front each frame by camera distance (stable tie-break on `id`).
- Reference handling (`lowerRef`/`upperRef`): GND/AMSL/AGL/FL/UNL are all treated as numeric ft above Y=0. UNL caps at the stored `upperFt` (typically 60000).

#### 3.2.1 Authoritative volumes (12, from AIP Thailand)
VTBD-CTR, VTBD-TMA, KPS-CTR, VTBP-CTR, VTP4, VTP7, VTR1, VTR2, VTR11, VTR13, VTR80, VTD21-1.

#### 3.2.2 Approximate volumes (133, flagged `approximate: true`)
Nationwide airport CTR/TMA overlays (including RTAF bases Takhli, Khorat, Kamphaeng Saen, Watthana Nakhon) plus **all ENR 5.1 P/R/D codes** parsed from `scripts/data/enr51_aip.txt` — polygons, circles, semi-circles, and TACAN arcs (arc/coast segments approximated). UI shows an `approx` badge; verify against current AIP before relying on boundaries.

> **Known data caveat:** VTR8 Kamphaeng Saen jettison polygon may disagree with AIP naming — marked `approximate: true`; re-verify against ENR 5.1 before publication. Coast-line and partial-arc boundaries are vertex-only approximations.

### 3.3 Drone — 6-DoF controls
- Position kept in `THREE.Vector3` separate from camera (clean reset semantics).
- **Body orientation** (`bodyYaw`, `bodyPitch`) drives movement; **camera orientation** (`yaw`, `pitch`) may diverge in locked view modes.
- Cruise speed from **absolute presets** (km/h), not abstract multipliers:
  | Preset | Label | Target speed |
  |---|---|---|
  | `1x` | 1× | 50 km/h |
  | `cessna172` | Cessna 172 | 226 km/h |
  | `learjet` | Learjet | 850 km/h |
  | `b777` | Boeing 777 | 920 km/h |
  | `100x` | 100× | 10 000 km/h |
- Default preset on load: **100×** (10 000 km/h).
- With `Shift`: an additional **3× boost** while held.
- Sim-speed buttons live in the HUD under **SIM SPEED**; the active preset name and target km/h are echoed in the SPD line.
- **Camera view modes** (press the same arrow again to return to front view):
  | Key | Mode | Behaviour |
  |---|---|---|
  | `↓` | Down (nadir) | Camera pitch ≈ −90°; screen crosshairs + nadir lat/lon overlay; mouse-look disabled |
  | `←` | Left | Camera yaw = body yaw − 90°; mouse-look disabled |
  | `→` | Right | Camera yaw = body yaw + 90°; mouse-look disabled |
- Inputs:
  | Input | Action |
  |---|---|
  | `W` / `S` | Move along the forward vector (horizontal when in down view) |
  | `A` / `D` | Strafe left / right along the horizontal right vector |
  | `Q` / `E` | Descend / ascend along world Y |
  | `Shift` | 3× boost on top of the selected preset |
  | HUD preset buttons | Set cruise speed preset |
  | `Space` | Toggle hover — freezes position; mouse-look continues (except in locked camera modes) |
  | `I` | Toggle **identify mode** — center crosshairs; volumes under the ray highlight (fill α=0.18, intentionally translucent so stacked hits remain individually readable); **bottom stacked info cards** show name, **distance to nearest point of the volume**, radius, base/ceiling. Cards sorted **nearest-first**. Suppressed while in down view (down view shows its own crosshairs). |
  | `↓` / `←` / `→` | Toggle down / left / right camera view (same key again → front view) |
  | `U` | Toggle unit system — `metric` shows m/m/s/km/km, `aero` shows ft/kt/NM/NM. Applies to HUD alt + speed rows, identify-card distance + base/ceiling, radar range-ring labels (50/100/200/300 km ↔ 25/50/100/200 NM), and the radar header. |
  | `M` | Toggle **map-primary view** — orthographic radar fills the viewport; 3D scene shrinks to a 320×240 bottom-right inset. WebGL renderer resizes via `ui.onMapPrimaryChange` callback. |
  | `+` / `-` | Zoom map in / out (alongside mouse scroll). Same clamp range as wheel (`400…8000 m/px`). |
  | `V` | Toggle **1st ↔ 3rd-person chase cam**. In 3rd-person, an aircraft mesh is rendered behind the body via `Drone._syncCamera()` using `THIRD_PERSON_BASE = {back:20, up:6}` × per-preset scale. **Aircraft model auto-swaps with `setSpeedPreset()`** — built from primitives in `src/drone.js` `buildAircraftModel(presetId)`: Mavic 3 / Cessna 172 / Learjet / Boeing 777 (scaled 0.55 for cuteness) / **UFO** (flying saucer, 100×). Works during tours too. |
  | `P` | Pause / resume the entire simulation. `drone.paused` gates `update()`; `tourGuide.update()` and `flyTo.update()` are stepped through `drone.update()` so freeze propagates. Mirrors the HUD **Pause** button. |
  | **Airplane mode** (Cessna 172 / Learjet / B777) | `Drone._updateAirplane(dt)`. **No stop / no reverse**: `airspeedMs` clamped `[minKmh × KMH_TO_MS, kmh × KMH_TO_MS × shiftBoost]` (Cessna 130, Learjet 240, B777 370 km/h floors). **Ailerons**: A/D = bank, `_targetRoll` ramps at 90°/s up to ±45°; `bodyRoll` lerps to target. **Yaw rate** from level-turn equation: `ω = g · tan(bank) / v`. W/S = throttle, Q/E = pitch. Mesh roll visible in chase cam via `mesh.rotation.z`. |
  | **Drone / UFO mode** (1× / 100×) | `Drone._updateFree(dt)` — original 6-DoF strafing preserved. |
  | Click | Request pointer-lock |
  | Mouse | Yaw + pitch on body (disabled while `cameraMode` is set) |
  | `Esc` | Release pointer-lock |
- Pitch clamp: ±85° (body pitch).
- Floor clamp: `position.y` ≥ 1 m.
- Stale-key protection: `window.blur` clears the held-key set (Alt+Tab no longer leaves a "stuck W").
- `flightLocked` during fly-to animations.

### 3.4 HUD (top-left panel)
- Lat/Lon as DMS using `formatLatLon`.
- Altitude in metres and feet AMSL.
- **Altitude reference tape** (`#altTape` canvas, **docked to right edge of `#hud` via `#telemetryStack` flex container, equal height** as of 2026-05-21 (d)): drawn by `UI._drawAltTape(altM)` each frame; canvas bitmap re-syncs to `clientWidth/clientHeight`. **Auto-zoom** stepped tops `[300, 600, 1k, 2k, 5k, 10k, 20k, 30k, 45k]` m chosen so scale top ≥ `altM × 1.6`. **Red dashed line at 90 m AGL** (CAAT recreational drone ceiling). Major ticks at one of `[50, 100, 200, 500, 1k, 2k, 5k, 10k]` m, minor ticks at 1/5 of major. **Reference bands** (per Thai aviation): Drone (0–295 ft), Heli ops (500–2k ft), GA/VFR (1k–10k ft), Jet climb (18k–28k ft), Airline cruise (30k–42k ft) — translucent fills with icon+label. Aircraft marker is a right-pointing triangle clamped to canvas; current altitude readout in canvas top-right. Honors `unitSystem`. Toggle via **Altitude** button in HUD.
- **Attitude indicator** (`#attitudeIndicator` 240×240 canvas, center-screen overlay, hidden by default): drawn by `UI._drawAttitudeIndicator(pitchRad, rollRad)`. Classic 6-pack — circular clip, brown-earth/blue-sky split that **rotates with bank** and **slides with pitch** (`pxPerDeg = r/30`). Pitch ladder every 5° (10° major with `|deg|` labels). Bezel bank scale at 0/±10/±20/±30/±45/±60° (yellow bezel triangle pointer at 0). Yellow aircraft-symbol bars + centre dot fixed to viewport. Toggle via **Attitude** button in HUD.
- **Pause button** (`#togglePause` in `#hudToggles`) — mirrors `P` key.
- **Ground-detail dropdown** (`#optGroundQuality` in Display options): Low/Med/High/Ultra (tile zoom 10/11/12/13, follow-range 2/2/3/4) + **Auto** (alt-adaptive via `ground.setAltitude()` called per-frame from `main.js`: high <500 m, med <3000 m, low above).
- **HDG row:** horizontal **slip-style** heading tape (`#hdgCompass` canvas, 176×44) — aviation-style flat band with gradient housing, edge vignette, vertical 5° ticks (30° numerals); **N/E/S/W cardinals** in large bold cyan above the tick baseline; **bottom lubber triangle** (cyan, pointing up) with no centerline; tape scrolls with **inertial smoothing** (exponential lag τ≈0.14 s); digital heading (`000° N`) on the right shows instantaneous value; hover suffix when active.
- Speed in m/s, km/h, active preset label, and preset target km/h.
- Sim-speed row: five preset buttons; active button highlighted.
- **Flight history:** Undo / Redo buttons with position index label.
- "Inside" chips with cached DOM updates.
- Hover indicator appended to heading line when hover is on.

### 3.5 Minimap / radar (bottom-left, 260×260 canvas)
- Top-down view; default ~300 km radius (scroll wheel zoom/pan).
- **Center aircraft** toggle (default **on** at startup): when on, radar center follows drone each frame; drag-pan disabled. When off: click & drag to pan; scroll wheel to zoom (400 m–8 km per px clamp); double-click recenters on drone.
- **Map underlay** toggle (default **on** at startup): OSM zoom-8 tiles drawn with **per-tile world bounds** and circular clip (pans with radar center).
- **FOV cone** toggle (default on): dashed triangle from the drone arrow, half-angle = camera FoV/2 (35°), reach 80 km — shows horizontal field of view (body heading).
- In **down view**, a crosshair is drawn on the drone marker on the radar.
- Range rings at 50/100/200/300 km, labelled on the east side.
- N/S/E/W compass ticks.
- Drone arrow at world position, rotated by body yaw.
- All catalogued airspaces drawn as filled+stroked top-down polygons.
- **Highlighted volumes** (identify mode or catalog fly-to): brighter fill, thicker category stroke, dashed accent outline on radar.

### 3.6 Educational panel (top-right, collapsible)
- **Panel title (`#panelTitle`):** live airspace name(s) the aircraft is inside (floor/ceiling when single volume); "Clear" when outside all volumes.
- **Display options:** **Show military airspaces (RTAF/RTN)** default **on**; toggle 3D airspace labels; toggle floor/ceiling text on labels.
- Legend mapping the 6 category colors to short explanations.
- **Thailand drone rules** — collapsible section (default **collapsed**); click heading to expand.
- **Filter box** above the airspace list (client-side text filter).
- Clickable list of every airspace; clicking starts a **warp fly-to** (`FlyToController`, **0.45 s** smoothstep) to a 40 000 ft AMSL overview framed to fit the volume; target volume highlights with fill + identify-style label. Position is pushed onto the flight-history stack on arrival.
- **No resume-flight row** — navigation history is handled by Undo/Redo in the HUD.
- Overview algorithm (`airspace.overviewVantage(id, fov)`):
  - Compute ring centroid + max radius `rmax`.
  - Altitude = 40 000 ft AMSL.
  - Camera distance = `max(rmax × 1.2 / tan(fov/2), rmax + 5 km, 8 km)` south of centroid.
  - Pitch/yaw aim at volume centroid.
- "Reset" button → recenter on Bangkok at 200 m AMSL, yaw=0, pitch=-0.05; clears highlights, flight history, and fly-to target.

### 3.6.1 Airspace Tour Guide
- Script data in `data/airspaceTour.json` — two variants: **`short`** (~5 min express) and **`full`** (~22 min country tour).
- Controller: `src/tourGuide.js` (`TourGuide` class) — chains `FlyToController` warps, locks manual flight, enables 3D labels for the duration.
- **Flow:** teleport to Bangkok → **takeoff** climb → smooth warp to each stop's `overviewVantage` → dwell with tourist-style narration → **finale** returns to Bangkok at 200 m with **Welcome to Explore**.
- **Short tour stops (10):** takeoff, VTBD-CTR, VTBD-TMA, VTR1, VTR2, VTP4, VTD17, VTR13, VTD21-1, finale.
- **Full tour stops (28):** takeoff, central airline/RTAF CTR/TMA, royal restricted, western danger, eastern naval/island, Phuket, Chiang Mai/Rai, Isaan, deep south, RTN prohibited sample, Khorat danger, in-flight drone-rules beat, finale.
- UI: panel buttons **Express tour** / **Full country tour**; overlay `#tourOverlay` with chapter, title, narration paragraphs, progress bar, **Skip stop** / **End tour**.
- Warp duration scales with distance (`flyDurationForDistance`); per-stop override via `flyDurationSec` in JSON.
- Tour completion pushes flight-history entry `Tour complete · explore`; manual catalog fly-to disabled while tour runs.
- **Cinematic dwell orbit (2026-05-21):** at every dwell stop the aircraft slowly orbits the airspace centre (vantage `lookX/lookZ`, falling back to Bangkok ORIGIN for non-airspace stops). Angular rate `(2π)/90 s`. Direction alternates per stop index. Orbit is cleared on `_runCurrentStop` so flying to the next stop is a clean warp, not a curve.

### 3.7 Flight history (Betterment-2 §10.5)

- `FlightHistory` maintains an undo/redo **ring buffer capped at 100 entries** of drone snapshots: position, body yaw/pitch, speed preset, hover, optional airspace label/id, trigger reason.
- **Event taxonomy** (`HISTORY_EVENT_TYPES` in `src/flightHistory.js`) — **additive only** (removing a type is a breaking change). Includes: preset change, mode change (Easy↔Realistic), throttle preset jump, pause/resume, RTH on/off, fly-to start/end, geofence boundary cross, alert state change (per §3.11 queue), course delta ≥ **15°** (cumulative since last entry), position delta ≥ **2 km** straight-line. **Excludes:** camera view toggle (V, ←, →, ↓), identify toggle, map-primary toggle, unit toggle, settings panel events. View-only changes do not count as "course changes."
- **UI:** collapsed by default — single-row strip `[▶ History 23]` in the HUD. Click to expand a scrollable card list (newest on top). Collapsed state persists to `localStorage`.
- Manual flight path sampled as XZ polyline for minimap/debug (2000 points max).
- Undo/Redo restores full drone state including camera mode and speed preset. Cursor displays `<current>/<total>`; reaching head/tail disables the corresponding button. When the ring trims an oldest entry, the cursor and any "future" redo entries clamp accordingly.
- Fly-to and reset push/pop stack entries; catalog warp pushes on completion.

### 3.8 Overlays
- **Place label** (`#placeLabel`): bottom-center reverse-geocoded Amphoe + Province via Nominatim (`src/geocode.js`); transparent background with text shadow for readability; cached on a ~100 m grid, throttled to ~1 req/s.
- **Crosshairs** (`#crosshairs`): visible in identify mode or down view.
- **Nadir info** (`#crosshairInfo`): lat/lon under crosshairs in down view.
- **View mode badge** (`#viewModeBadge`): prompts to press the same arrow again for front view (down / left / right).
- **Identify panel** (`#identifyPanel`): bottom-center stacked cards when identify mode hits volumes — each card shows **name**, **radius** (NM or approximate max extent), **base/ceiling** (ft AMSL), and category badge; background tint matches airspace category (CTR/TMA/Class D/P/R/D).

### 3.9 Controls hint (bottom-center)
- Shows full keyboard cheatsheet for 5 seconds, then auto-collapses to a `?` button.
- Click to re-expand.

### 3.10 Mobile fallback
- `matchMedia("(pointer: coarse)") && !matchMedia("(pointer: fine)")` → show "Desktop required" notice.

### 3.11 Alert priority queue (Betterment-2 §10.3)

A **single** alert banner renders at any time. Lower-tier alerts remain visible as chips (so the operator can see they exist) but do not own the banner. The queue is the single source of truth — no module writes directly to the banner DOM.

**Priority (high → low):**

1. `NO_FLY` — Prohibited / Restricted active hours
2. `AUTH_CLAMP` — Class D / TMA above authorisation ceiling (only fires when Strict CAAT is **on**, §3.12)
3. `RTH_ACTIVE` — banner reads `RTH ENGAGED (${triggerReason})`; reason is one of `battery` / `link` / `manual` and re-evaluates each frame (closes external P0 #3 banner-staleness bug)
4. `ALT_OVER_REG` — above regulated ceiling for the active preset
5. `ALT_OVER_OP` — above operational ceiling for the active preset
6. `ALT_AT_REG` / `ALT_AT_OP` — approaching ceiling (≥ 90% — hysteresis exit at 85%)
7. `ADVISORY` — within 5 NM of CTR/TMA, low battery, link degraded

**Module:** `src/alerts.js` — emitters publish `{tier, key, message, sourceTs}` objects; the renderer selects `max(by priority)` and writes to `#alertBanner`. Subscribers (HUD chips, history) receive the full list.

### 3.12 Strict CAAT mode (Betterment-2 §10.1)

A single **boolean contract** that gates all altitude / geofence physical enforcement. The default is **OFF** — every preset (drone, GA, jet, airliner, UFO) flies wherever the operator commands, with the alert system reporting violations advisorily. This is required for the Airspace Tour to traverse Class D/TMA/Restricted without being silently clamped.

- **State:** `simState.strictCaat: boolean`, persisted to `localStorage` under key `kuson.sim.strictCaat`.
- **Accessor:** `simState.isStrictCaat()` — the **only** sanctioned read path. No scattered `if (window.__sim.strict)` checks. Code that observes the flag through any other route is a defect.
- **UI:** visible HUD toggle on the telemetry column (top or bottom — confirmed during P1.T4 smoke). Label: `CAAT: OFF` / `CAAT: ON`. Not buried in Settings.
- **Effects when ON:**
  - The `AUTH_CLAMP` alert tier becomes enforceable for the **Mavic 3 preset only** inside controlled airspace — re-applies the 120 m AGL clamp in `src/altitudeAdvisor.js` (not in `failures.js`).
  - Other presets remain warning-only even with the toggle on — a Cessna in VTBD-TMA still receives an `ALT_OVER_REG` banner, never a clamp.
- **Effects when OFF (default):**
  - No altitude clamp from any source.
  - The pre-Betterment-2 unconditional 120 m AGL clamp in the geofence path is retired. Its emission point is now an event `geofence:authCeilingExceeded` consumed by the alert queue.

---

## 4. Non-functional requirements

| Concern | Decision |
|---|---|
| Build / bundling | None — ES modules + importmap |
| Three.js delivery | `https://unpkg.com/three@0.170.0/build/three.module.js` |
| Tile delivery | `https://{a,b,c}.tile.openstreetmap.org/{z}/{x}/{y}.png` — zoom 9 base + zoom 11 detail |
| Geocoding | Nominatim reverse API (`nominatim.openstreetmap.org`), throttled + cached |
| Geolocation | `navigator.geolocation.getCurrentPosition` with Thailand bounds check; 12 s timeout |
| Offline behavior | Falls back to procedural dark ground if tiles fail; airspaces load from local JSON; geocode shows "…" offline |
| Frame rate target | 60 FPS on a 2020-era laptop iGPU |
| Render stability | Shaded translucent walls with shared materials + outline pool (Betterment-1 P2.T7 / P5.T5); `logarithmicDepthBuffer`; fixed camera clip; detail tiles follow drone; baked minimap polygons (P5.T4); dynamic DPR under frame pressure (P5.T8) |
| Error UX | Catch in bootstrap; loading splash text replaced with a failure message |
| Debug surface | `window.__sim = { scene, camera, drone, layer, ground, flightHistory, tourGuide, simState, altitudeAdvisor, alerts }` |

---

## 5. File layout

```
kuson_thailandairspace_sim/
├── index.html                # entry: UI shell, importmap, styles, loading splash
├── src/
│   ├── main.js               # Three.js scene + ground + compass + bootstrap loop
│   ├── ground.js             # DynamicGround (zoom-9 base + zoom-11 detail tiles)
│   ├── geocode.js            # Nominatim reverse geocode (Amphoe, Province)
│   ├── geolocation.js        # Browser GPS startup with Thailand bounds check
│   ├── drone.js              # 6-DoF controls, speed presets, camera modes, pointer-lock
│   ├── airspace.js           # JSON loader, wireframe volumes, highlight fills, labels
│   ├── flyto.js              # FlyToController — smooth catalog warp fly-to
│   ├── tourGuide.js          # Scripted airspace tour (short + full variants)
│   ├── flightHistory.js      # Undo/redo stack + path samples
│   ├── identify.js           # Center-ray pick + screen-space label layout
│   ├── coords.js             # lat/lon ↔ world XZ, tile math, DMS format
│   └── ui.js                 # HUD, radar pan/zoom/center, panel, history buttons
├── data/
│   ├── airspaces.json        # 144-volume nationwide catalog (ENR 2.1 + ENR 5.1)
│   └── airspaceTour.json     # Tour Guide scripts (short + full)
├── scripts/
│   ├── build_airspaces.py    # Regenerate airspaces.json from AIP + curated data
│   └── data/                 # enr51_aip.txt, curated_prd.json
├── spec.md                   # this document
├── journal.md                # append-only log of coding/decisions
├── state_TODO.md             # single source of truth: state + task checklist
├── claude.md                 # required Claude skills for this project
├── README.md                 # user-facing run + extension instructions
└── LICENSE                   # MIT
```

---

## 6. Acceptance criteria

A reviewer should be able to verify Phase 1 by:

1. `python3 -m http.server 8000` from the project root.
2. Open `http://localhost:8000/index.html` in a modern desktop browser.
3. **Loading splash disappears** within ~2 s and the HUD appears (GPS prompt may appear first).
4. **OSM tiles render** within ~5 s at startup location; fallback plane visible until tiles load.
5. Click the scene → cursor lock acquired; WASD/QE move the drone; mouse looks.
6. Press `Space` → "HOVER" appears in the heading line; **all WASD/QE input is ignored** until pressed again.
7. Press `Esc` → cursor releases; LMB-drag also looks (fallback).
8. Click any airspace in the right-side list → **0.45 s warp** to 40 000 ft overview; volume highlights with label; flight-history counter increments.
9. Click **Undo** → returns to pre-warp position; **Redo** restores warp destination.
10. Press `I` → center crosshairs; airspaces under the view axis highlight; bottom cards show name, radius, base/ceiling with category colors; press `I` again to exit.
11. Press `↓` → down view with crosshairs and nadir lat/lon; radar shows crosshair on drone; press `↓` again for front view.
12. Press `←` / `→` → left/right view badges; same key again for front view.
13. Fly into VTR1 Bangkok City (the inner-city 10 NM ring at ≤3000 ft) → its chip appears; panel title updates; fly out → chip/title clear.
14. Press `Reset` → drone returns to Bangkok center at 200 m AMSL; history cleared.
15. Window resize → renderer/camera adapts.
16. Alt+Tab while holding `W`, switch back → drone does **not** keep flying forward.
17. Click sim-speed presets → SPD line shows preset name and target km/h; **100×** default is visibly fast.
18. HDG slip compass: large cyan N/E/S/W, bottom lubber triangle (no centerline), inertial tape lag; digital degrees update immediately.
19. Toggle **3D airspace labels** → sprite labels appear at volume centroids; floor/ceiling toggle updates label text.
20. Toggle **Map underlay** on radar → OSM tiles appear beneath airspace polygons (on by default).
21. **Center aircraft** on by default; uncheck to drag-pan radar; recheck to follow.
22. FOV cone visible on radar (dashed triangle); toggling off hides it.
23. Radar: scroll to zoom; drag to pan when center-aircraft off; double-click to recenter.
24. **Place label** (bottom-center) populates with Amphoe, Province after ~1 s (requires network for Nominatim).
25. Military airspaces visible on first load (display option checked by default).
25. Fly through nested CTR/TMA → no transparency z-fighting flicker (highlight fill only when active).
26. Identify mode (`I`): highlighted volumes also show brighter fill + dashed accent outline on radar.
27. Horizon N/E/S/W labels stay on the compass ring around the aircraft while flying (e.g. warp to Phuket).
27. **Thailand drone rules** collapsed by default; click heading to expand.
28. Filter box narrows the airspace list client-side.

---

## 7. Glossary

| Term | Meaning |
|---|---|
| AGL | Above Ground Level |
| AIP | Aeronautical Information Publication |
| AIRAC | Aeronautical Information Regulation And Control (publication cycle) |
| AMSL | Above Mean Sea Level |
| ARP | Aerodrome Reference Point |
| CAAT | Civil Aviation Authority of Thailand |
| CTR | Control Zone |
| ENR | En-Route section of an AIP |
| FL | Flight Level (e.g., FL240 ≈ 24 000 ft at standard pressure) |
| FoV | Field of View |
| NBTC | National Broadcasting and Telecommunications Commission (TH) |
| NM | Nautical Mile (1852 m) |
| TMA | Terminal Manoeuvring Area |
| UNL | Unlimited (no defined ceiling) |
| VLOS | Visual Line Of Sight |
| VTBD / VTBS / VTBU / VTBP | ICAO codes for Don Mueang / Suvarnabhumi / U-Tapao / Hua Hin |

---

## 8. Source data

- AIP Thailand ENR 2.1 (FIR/UIR/TMA, 2021-08-12 AIRAC)
- AIP Thailand ENR 5.1 (Prohibited/Restricted/Danger, 2020-11-05 AIRAC)
- Both at <https://aip.caat.or.th/>.
- `scripts/build_airspaces.py` merges AIP extracts with `scripts/data/curated_prd.json` for nationwide airport CTR/TMA approximations.
- Approximate volumes' polygons are interpreted from DDMMSS notation and explicitly marked `approximate: true` with a source note.

---

## 9. Implementation notes (Betterment, 2026-05-28 → 05-29)

Sections 1–8 are the Phase-1 design record. The "Betterment" effort (phase
tags `betterment-phase-0-complete` … `-6-complete`, then `betterment-complete`)
extended the sim well past that scope. This section documents *what was built*,
verified against the code.

### 9.1 Flight modes (`src/modes.js`, `src/drone.js`)

- **Easy Mode (Hovercraft)** — free 6-DoF, no inertia/stall. Default. Toggle `K`.
- **Realistic Mode** — second-order flight models per preset. The UFO (100×)
  always hovercrafts regardless of mode.
- `resolveMode(presetId, EasyMode.enabled)` maps preset → `FlightMode`
  (`HOVERCRAFT` / `DRONE` / `AIRPLANE` / `UFO`).

### 9.2 Physics models (`src/physics.js`)

**QuadrotorModel** (Mavic 3, DRONE): tilt-to-translate with actuator lag,
horizontal drag, and a commanded-climb integrator. Tilt scale ×1.5 on Shift,
÷2 on Ctrl.

**FixedWingModel** (AIRPLANE) — lift/drag/thrust with stall + energy trade.
Per-preset aero constants:

| Preset | `kmh` cruise | `minKmh` | `Vs` (m/s) | `Vne` (m/s) | `clMax` | `cd0` |
|---|---|---|---|---|---|---|
| Cessna 172 | 226 | 130 | 23.5 | 80 | 1.4 | 0.027 |
| Learjet 35 | 850 | 240 | 47 | 195 | 1.6 | 0.020 |
| Boeing 777 | 920 | 370 | 71 | 280 | 1.8 | 0.018 |

(`kInduced = 0.013`.) Below `1.05·Vs` the nose drops at a fixed rate and the
aircraft loses altitude. Yaw rate from bank uses the level-turn equation
`ω = g·tan(bank)/max(v, Vs)`. Speed presets: Mavic 50 km/h, UFO 10 000 km/h.

**Wind** (`src/wind.js`) — a time-varying field applied as a ground-velocity
bias in Realistic modes only; the minimap draws nose vs ground-track.

**Input** (`src/input.js`) — deadzone + expo shaping; gamepad polled per
substep (Mode 1/2 mapping, persisted to `localStorage`). Fixed 120 Hz physics
substep with an accumulator.

### 9.3 Drone systems (`src/failures.js`) — pure, unit-testable

- **BatterySystem** — drain by activity: hover 22 min, cruise 28 min,
  max-throttle 15 min to empty. State bands green ≥30 % / yellow 20–30 % /
  red <20 %. Resets after 2 s ground contact.
- **ReturnToHome** — state machine INACTIVE→ASCEND(60 m AGL)→FLY_HOME→DESCEND→
  LANDED. Triggers: battery <25 %, signal-loss >3 s, or manual (`R`).
- **RadioLink** — `quality = clamp(1 − distKm/10)`, bars 5→0; Poisson dropouts
  (0.5–3 s) below 50 % quality; contiguous loss feeds RTH.
- **Geofence** — tiers *advisory* (≤5 NM lateral of CTR/TMA), *authorisation*
  (inside Class D/TMA → clamp to ground+120 m AGL), *no-fly* (Prohibited or
  `isMilitaryAirspace` → revert to last-safe position). Evaluated against
  `airspacesAtUnfiltered` so hidden military zones still enforce.

### 9.4 Terrain (`src/terrain.js`, `scripts/bake_terrain.py`)

`elevationAt(lat,lon)` / `AGL(pos)` read a 30 arc-sec grid baked from **AWS
Terrain Tiles (Mapzen Terrarium PNG)** at zoom 10, covering 5.6–20.5°N ×
97.3–105.7°E → `data/terrain.bin` (1788×1008 little-endian Uint16 metres,
row-major south→north / west→east, ~3.6 MB) + `data/terrain.json` metadata.
Bilinear sample; falls back to AMSL until the binary loads. Doi Inthanon's
2 565 m summit bakes to ~2 561 m.

### 9.5 Atmosphere (`src/sky.js`)

The flat-gradient sky was replaced by the three.js `Sky` shader (Rayleigh/Mie
scattering) with a shared sun direction driving a sun-disc sprite + a
`DirectionalLight`; fog far-plane is lerped by altitude.

### 9.6 Architecture & performance (Phase 5)

- **SimMode** (`src/simMode.js`) — single state machine
  (`free/flyingTo/touring/paused/replay`) with validated transitions; the loop
  reads one mode instead of four booleans.
- **Per-airspace AABB** reject before point-in-ring in `airspacesAt` /
  `airspacesAtUnfiltered` / `predictNextEntry`.
- **Identify ray** throttled to 10 Hz; the march replaced by an exact
  **analytical ray-vs-prism** test (catches thin layers).
- **Minimap** static polygons **baked to a 2048² offscreen canvas** and
  blitted per frame (re-bake only on filter/highlight/pan/zoom change). Tile
  cache holds 256 tiles (> the 121-tile working set — fixes a flicker bug).
- **Shared airspace materials** (wall singletons + outline pool; highlight =
  reference swap) and **dynamic DPR** reduction under frame pressure.

### 9.7 Pedagogy / HUD additions

AGL chip + terrain-relative 90 m line; battery / link / predictive
"next airspace in Ns" chips; tiered-geofence ribbons + no-fly toast;
colorblind hatch/dots on Prohibited/Restricted — both the radar fills and
the 3-D walls (the latter a procedural world-space pattern injected into
the wall material via `onBeforeCompile`, avoiding ExtrudeGeometry's
world-scale-UV noise); identify-mode floating 3D labels; 8-direction
vantage compass-rose per airspace card.

---

## 10. Betterment-2 architecture (2026-05-29)

Implements [20260529_betterment2.md](20260529_betterment2.md), which addresses operator field reports E1–E4 and external audit P0 items #1, #3, #4, #5, #7. Items P1/P2 from the external audit are parked in [20260529_todo.md](20260529_todo.md) `Discovered` section.

### 10.1 Strict CAAT contract (`simState.strictCaat`)

See §3.12. The single architectural seam for all altitude / geofence physical enforcement. Read through `simState.isStrictCaat()` only.

**Migration:** the unconditional 120 m AGL clamp inside `failures.js` `Geofence.evaluate()` is removed. Its emission point becomes an event `geofence:authCeilingExceeded` published to the alert queue (§3.11). The clamp re-applies only inside `src/altitudeAdvisor.js` and only for the Mavic 3 preset when `isStrictCaat() === true`.

### 10.2 Altitude advisor (`src/altitudeAdvisor.js`)

Owner of the altitude-state machine; no altitude logic in `failures.js`, `drone.js`, or `physics.js`.

- **States per preset:** `NORMAL → AT_OP → OVER_OP → AT_REG → OVER_REG`.
- **Hysteresis:** enter `AT_*` at 90% of ceiling, enter `OVER_*` at 100%, leave `AT_*` at 85%, leave `OVER_*` at 95%. Prevents banner flapping at the boundary.
- **Per-preset ceilings** (§10.2.1). Editable at runtime in Settings; persisted to `localStorage`; reset-to-default button per row.
- **Outputs:** publishes `ALT_AT_OP` / `ALT_OVER_OP` / `ALT_AT_REG` / `ALT_OVER_REG` events to the alert queue. Each carries a `descendTo` value used in the banner message (e.g. `OVER REGULATED — descend to 10 000 ft`).

#### 10.2.1 Default ceiling table

| Preset | Class | Operational ceiling | Regulated ceiling | Rationale |
|---|---|---|---|---|
| Mavic 3 | Consumer drone | 6 000 m AMSL (svc) | 120 m AGL (CAAT) | DJI service ceiling / CAAT §2 hobbyist rule |
| Cessna 172 | GA piston | 4 267 m AMSL (14 000 ft) | 3 048 m AMSL (10 000 ft / no O₂) | Type cert / FAA-CAAT supplemental-O₂ |
| Learjet 35 | Light biz-jet | 13 716 m AMSL (45 000 ft) | 12 497 m AMSL (FL410) | Type cert / RVSM upper |
| Boeing 777 | Heavy airliner | 13 137 m AMSL (43 100 ft) | 12 497 m AMSL (FL410) | Type cert / RVSM upper |
| UFO (100×) | N/A | `Infinity` | `Infinity` | Joke preset; no warnings |

### 10.3 Alert queue (`src/alerts.js`)

See §3.11 for the priority list. Single subscriber renders to `#alertBanner`; lower-tier alerts surface as chips. The queue replaces the betterment-1 ad-hoc banner stack observed at Sattahip (RTH + ADVISORY + NO-FLY firing simultaneously with a stale RTH reason).

**API surface:**

```
alerts.publish({ tier, key, message, descendTo?, sourceTs })
alerts.retract(key)
alerts.subscribe(listener)  // listener({active, chips})
alerts.isStrictCaat()        // re-export of simState accessor for convenience
```

### 10.4 Sky shader floor lift (`src/sky.js`)

Rayleigh/Mie extinction constants retuned so the dome luminance ≥ 55% of sea-level at any altitude up to 60 km AMSL. Above ~100 km it correctly fades to black. Documented in the journal block for P4.T1 (revertable constants).

Closes operator field report E3 first half — the "dark as space" observation occurred at airliner altitudes where the betterment-1 shader was already physically transitioning to vacuum.

### 10.5 Flight history rewrite (`src/flightHistory.js`)

See §3.7. The full module is rewritten around `HISTORY_EVENT_TYPES` and a 100-entry ring buffer. The undo/redo cursor bug from external audit Medium #7 is closed by clamping the cursor against the ring on every push.

### 10.6 Fly-to lockout transparency (`src/flyto.js`)

Pre-betterment-2, a user-initiated fly-to click would silently no-op when a state guard (`flightLocked`, tour active, RTH active) was set. After betterment-2 P1.T1/T2:

- A click on an airspace card **always** produces either a fly-to start or a toast naming the lockout reason ("Tour in progress — end tour to fly to volume").
- The state machine is documented in `doc/flyto_state_machine.md` as the canonical reference; future guards must be added there.

Closes operator field report E4.

### 10.7 Identify card cap (`src/identify.js`, `src/ui.js`)

The bottom identify panel shows the top **3 nearest** cards plus a `+N more — expand` row. Expansion reveals the remainder in a scrollable section that does not grow the bottom strip beyond its capped height. Closes external audit P0 #4.

### 10.8 Ground detail (`src/ground.js`, `src/cities.js`, `src/provinces.js`)

- Default Carto Positron zoom raised one step at "Medium" (z11 → z12).
- Province boundary overlay opacity 0.35 → 0.55, line width +1.
- City beacon sprites +30% scale; each gains a colored halo (Bangkok = magenta, Chiang Mai = cyan, etc — list maintained in `src/cities.js`).

Closes operator field report E3 second half.
